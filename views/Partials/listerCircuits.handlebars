<table class="tabpayscir">
    {{#each listeCircuit}}
        <tr><td><img src="/image/pays/{{this.payadrdrap}}" alt="drapeau du pays" class="drapeaux" />
            <a href="/detailDuCircuit/{{this.cirnum}}">{{this.cirnom}}</a>
        </td></tr>
    {{/each}}
</table>
<br class="myboth" />